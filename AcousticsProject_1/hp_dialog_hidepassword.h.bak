#ifndef HP_DIALOG_HIDEPASSWORD_H
#define HP_DIALOG_HIDEPASSWORD_H

#include <QWidget>
#include <QMouseEvent>

#include "hp_input_text.h"
#include "hp_assemblystyle.h"

namespace Ui {
class hp_dialog_hidepassword;
}

class hp_dialog_hidepassword : public QWidget
{
    Q_OBJECT

public:
    explicit hp_dialog_hidepassword(QWidget *parent = nullptr);
    ~hp_dialog_hidepassword();

    int UIType;

    void UIStatusUpdate(bool isPasswordSelect,bool isButtonSelect);
    QString key_adjust;
    QString key_calibration_ir;

   ir_hzh IRHZHPara;
protected:
    virtual bool eventFilter(QObject *obj, QEvent *event);

signals:
    void SignalHpHideInterfaceShow(int data);//1 进入adjust界面  2进入calibration红外配准界面
    void SignalUpdateSetUpClock();

private slots:
    void hp_slot_text_over(QString text,INPUT_NOW_SHOW nowShow);

    void on_toolButton_enter_clicked();
    void on_toolButton_close_clicked();

private:
    Ui::hp_dialog_hidepassword *ui;

    void changeEvent(QEvent *e);
    hp_input_text *HpInputText;

};

#endif // HP_DIALOG_HIDEPASSWORD_H
